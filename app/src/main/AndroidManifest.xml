<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.lidy.demo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".SampleApplication"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".viewpage.PageActivity" />
        <activity android:name=".DrawActivity" />
        <activity android:name=".rx.TestSimpleActivity"/>
        <activity android:name=".animator.AnimatorActivity"/>
        <activity android:name=".viewpage.TwoPageActivity"/>
        <activity android:name=".example.ExampleActivity"/>
        <activity android:name=".motion.MotionExampleActivity"/>
        <activity android:name=".drag.DragActivity"/>
        <activity android:name=".viewpage.PageListActivity"/>
        <activity android:name=".coordinator.CoordinatorActivity"/>
    </application>

</manifest>